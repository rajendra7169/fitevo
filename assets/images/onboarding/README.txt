Drop the onboarding splash hero image here (optional).

  splash.jpg   <- shown on the welcome screen

Sizing: portrait 3:4, ~1080 px wide, compressed to ~200 KB.
The app falls back to a gradient if missing.
